import React from "react";
import { Card } from "../ui/widgets";

export default function Calendar() {
  return (
    <div className="mt-6">
      <Card>
        <div className="text-sm text-slate-600">Calendar page (optional). Add events UI if required.</div>
      </Card>
    </div>
  );
}